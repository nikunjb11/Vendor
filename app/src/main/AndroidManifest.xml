<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.softhub.vendor">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".AddNewCustomer"></activity>
        <activity android:name=".MasterPackage" />
        <activity android:name=".UserSubscriptionActivity" />
        <activity android:name=".SignUp" />
        <activity android:name=".AddAddressActivity" />
        <activity android:name=".OnlineApproveActivity" />
        <activity android:name=".OnlineRequestActivity" />
        <activity android:name=".CODApproveActivity" />
        <activity android:name=".CODRequsetActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateItem" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".AddPackageActivity" />
        <activity android:name=".AddCatagoryActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UserActivity" />
    </application>

</manifest>